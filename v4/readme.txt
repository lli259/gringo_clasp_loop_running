use control.py to start, stop, and restart, kill three groups of gringo and clasp processes.

Check clasp process status each second. Stop once one clasp finishes (stop flag set to 1).
Or continue next encoding when reach 20 seconds.
